let age = 18;
if(age >= 18) {
    if(age == 18) console.log(`User is able to vote for the first time`);
    else console.log(`User is able to vote`);
}else console.log(`User is unable to vote`);

let isStudent = false;
if(isStudent) console.log(`User is a student`);
else console.log(`User is not a student`);